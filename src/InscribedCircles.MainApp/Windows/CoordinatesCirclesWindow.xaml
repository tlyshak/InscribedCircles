<telerik:RadWindow x:Class="InscribedCircles.MainApp.Windows.CoordinatesCirclesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
        Header="Координати кіл" Height="300" Width="300" WindowStartupLocation="CenterScreen">
    <Grid>
        <!--<telerik:RadBusyIndicator IsBusy="{Binding IsBusy}" >
            <telerik:RadGridView ItemsSource="{Binding Points}" ShowColumnSortIndexes="False" ShowGroupPanel="False" 
                             RowIndicatorVisibility="Collapsed" ColumnWidth="*" IsFilteringAllowed="False"/>
        </telerik:RadBusyIndicator>-->
        <DataGrid ItemsSource="{Binding Points}"></DataGrid>
    </Grid>
</telerik:RadWindow>
